
# DNDX — Ultra Premium Clean Skeleton

**What’s included**
- Vite + React + TypeScript + Tailwind + Framer Motion
- Matrix intro runs once (stored under `dndx_intro_seen_v1`), black bg + white % + rain in gold/deep blue
- Premium navbar with smooth gradient underline and proper active state
- Consistent custom cursor (never switches on hover)
- Section slide-up transitions
- Pure deep black base with a subtle gold glow and deep blue accents

**Run**
```bash
npm install
npm run dev
```
