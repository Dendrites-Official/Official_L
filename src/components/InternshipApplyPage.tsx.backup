import React, { useState, useRef } from 'react';
import { useParams, Link } from 'react-router-dom';
import Footer from './Footer';

type RoleKey = 'frontend' | 'backend' | 'marketing';

export default function InternshipApplyPage() {
  const { roleId } = useParams<{ roleId: string }>();
  
  return (
    <div className="min-h-screen bg-[#020617] text-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-8 py-8 sm:py-12">
        <h1 className="text-3xl font-bold">Application Page - {roleId}</h1>
        <p className="text-white/70 mt-4">Form will be here</p>
        <Link to="/careers" className="text-blue-400 mt-4 inline-block">Back to Careers</Link>
      </div>
      <Footer showCompanyInfo={false} />
    </div>
  );
}
